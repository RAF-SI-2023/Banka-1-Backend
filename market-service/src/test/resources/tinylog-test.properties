writer          = rolling file
writer.level    = info
writer.format = {date:yyyy-MM-dd HH:mm:ss}     {class-name}.{method}()     {level}: {message}
writer.file     = src/main/resources/logs/test/log_{count}.txt
